  // Relación entre Empleado y solicitarTienda
// Empleado.hasMany(solicitarTienda, {
//   foreignKey: 'empleadoId', // La clave foránea en la tabla solicitarTienda
//   sourceKey: 'id', // La clave primaria en la tabla Empleado
// });

// solicitarTienda.belongsTo(Empleado, {
//   foreignKey: 'empleadoId', // La clave foránea en la tabla solicitarTienda
//   targetKey: 'id', // La clave primaria en la tabla Empleado
// });

// // Relación entre Cliente y solicitarTienda
// Cliente.hasMany(solicitarTienda, {
//   foreignKey: 'clienteId', // La clave foránea en la tabla solicitarTienda
//   sourceKey: 'id', // La clave primaria en la tabla Cliente
// });

// solicitarTienda.belongsTo(Cliente, {
//   foreignKey: 'clienteId', // La clave foránea en la tabla solicitarTienda
//   targetKey: 'id', // La clave primaria en la tabla Cliente
// });

// // Relación entre Producto y solicitarTienda
// Producto.hasMany(solicitarTienda, {
//   foreignKey: 'productoId', // La clave foránea en la tabla solicitarTienda
//   sourceKey: 'id', // La clave primaria en la tabla Producto
// });

// solicitarTienda.belongsTo(Producto, {
//   foreignKey: 'productoId', // La clave foránea en la tabla solicitarTienda
//   targetKey: 'id', // La clave primaria en la tabla Producto
// });